// This component is no longer used since we switched back to the Git clone tool functionality
// It's kept for reference only

/*
import { useEffect, useRef, useState } from "react"
import { ChatProps, Message, User } from "../interfaces"
import { socket } from "../services/socket";
import Header from "./chat/Header";
import Notification from "./chat/Notification";
import Sidebar from "./chat/Sidebar";
import { FiSend, FiImage, FiSmile, FiPaperclip } from "react-icons/fi";
import MessageComp from "./chat/MessageComp";

const Chat = ({ currentUser, onLogout }: ChatProps) => {
    // This component is no longer used
    return <div>Chat component replaced by Git Clone Tool</div>;
};

export default Chat;
*/